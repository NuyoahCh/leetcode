# 官方链接
https://leetcode.cn/problems/delete-columns-to-make-sorted-ii/description/?envType=daily-question&envId=2025-12-21

## 题解
https://leetcode.cn/problems/delete-columns-to-make-sorted-ii/solutions/3862460/go-mei-ri-yi-ti-by-code-review-ktmw/

## Code
```go
func minDeletionSize(A []string) int {
    N := len(A)
    W := len(A[0])
    ans := 0
    // cur : all rows we have written
    // For example, with A = ["abc","def","ghi"] we might have
    // cur = ["ab", "de", "gh"].
    cur := make([]string, N)
    for j := 0; j < W; j++ {
        // cur2 : What we potentially can write, including the
        //        newest column col = [A[i][j] for i]
        // Eg. if cur = ["ab","de","gh"] and col = ("c","f","i"),
        // then cur2 = ["abc","def","ghi"].
        cur2 := make([]string, N)
        copy(cur2, cur)
        for i := 0; i < N; i++ {
            cur2[i] += string(A[i][j])
        }
        if isSorted(cur2) {
            cur = cur2
        } else {
            ans++
        }
    }

    return ans
}

func isSorted(A []string) bool {
    for i := 0; i < len(A) - 1; i++ {
        if A[i] > A[i + 1] {
            return false
        }
    }
    return true
}
```